name: Compile SCSS

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  build_css:
    runs-on: ubuntu-latest

    steps:
      - name: Sass Build
        uses: gha-utilities/sass-build@v0.3.4
        with:
          source: scss/styles.scss
          destination: css/styles.min.css
#       - name: Checkout source Git branch
#         uses: actions/checkout@v2
#         with:
#             ref: src-pages
#             fetch-depth: 10
#             submodules: true

#       - name: Make destination directory for compiled CSS
#         run: mkdir -vp /tmp/os-and-xs/assets/css
#         # run: mkdir -vp /os-and-xs/css

#       - name: Compile CSS from SCSS files
#         uses: gha-utilities/sass-build@v0.3.4
#         with:
#           source: /os-and-xs/scss/styles.scss
#           destination: /os-and-xs/css/styles.min.css

#       - name: Checkout destination Git branch
#         uses: actions/checkout@v2
#         with:
#             ref: pr-pages
#             fetch-depth: 1

#       - name: Move compiled CSS to path within pr-pages branch
#         run: mv /os-and-xs/css

#       - name: Add and Commit changes to pr-pages branch
#         run: |
#           git config --local user.email 'action@github.com'
#           git config --local user.name 'GitHub Action'
#           git add os-and-xs/css/*
#           git commit -m 'Updates compiled CSS files'

#       - name: Push changes
#         uses: ad-m/github-push-action@master
#         with:
#           github_token: ${{ secrets.GITHUB_TOKEN }}
#           branch: pr-pages

#       - name: Initialize Pull Request
#         uses: gha-utilities/init-pull-request@v0.0.4
#         with:
#           pull_request_token: ${{ secrets.GITHUB_TOKEN }}
#           head: pr-pages
#           base: gh-pages
#           title: 'Updates site files from latest Actions build'
#           body: >
#             Perhaps a multi-line description
#             about latest features and such.

# - name: Sass Build
#   uses: gha-utilities/sass-build@v0.3.4
#   with:
#     source: scss/styles.scss
#     destination: css/styles.min.css
